# 디자인 기준

이 문서는 "AI가 찍어낸 티가 나는 화면"을 피하기 위한 우리 앱의 규칙이에요.
새로 화면을 만들거나 고칠 때 이 문서를 먼저 읽어주세요.
(사람이든 AI든 마찬가지예요. 이 문서가 없으면 또 기본값 티가 나는 화면이 나와요)

## 읽은 글

| 글 | 한 줄 |
|---|---|
| [토스 10년, 쉬운 금융이 만든 혁신과 그늘](https://ditoday.com/토스-10년-쉬운-금융이-만든-혁신과-그늘/) | 한 화면에 판단 하나, 여백 크게, 군더더기 단어 걷어내기. 단 쉬움이 은폐가 되면 안 됨 |
| [AI가 만든 '싼티' 나는 UI 피하는 법](https://hgko-dev.tistory.com/555) | 4px 간격 체계, 44px 터치 영역, 모듈러 타입 스케일, 틴티드 뉴트럴 |
| [AI 기운 빼기](https://doyouknowcom.tistory.com/66) | 색은 직접 고르기, 레퍼런스 주기, 실제로 렌더링해서 검수하기 |
| [taste-skill](https://best-onedevyjuns.tistory.com/147) | 같은 카드 반복 금지, 순수 검정·네온 금지, 밀도/모션/변주를 의식적으로 조절 |
| [AI 랜딩페이지 아마추어 느낌 제거](https://brunch.co.kr/@acc9b16b9f0f430/240) | 기준(색·폰트·여백)을 먼저 문서로 만들고 그 위에서 작업 |
| [인공지능이 가져올 궁극의 UI 혁명](https://yozm.wishket.com/magazine/detail/3271/) | '모먼트' — 필요한 순간에 딱 필요한 동작만 요구하기. 마찰 줄이기 |
| [2025 UX/UI 트렌드 5가지](https://kr.linkedin.com/pulse/future-uxui-design-5-game-changing-trends-shaping-2025-julee-gami-xqrdf) | 능동적 제안, 입체감, 접근성, 감정적 인터랙션, 깊이와 명료성 |

## 관통하는 원칙 8가지

1. **한 화면에 판단 하나.** 홈에서 물어볼 것은 "지금 뭐 하지?" 하나예요.
   나머지는 다음 화면으로 밀어요.
2. **필요한 순간에 필요한 것만.** 오늘 쓰지 않을 정보는 홈에 두지 않아요.
3. **여백이 정보다.** 빽빽하게 채우는 것보다 덜어내는 게 어려워요.
4. **네모 창을 쌓지 않는다.** 테두리 카드를 겹겹이 두면 화면이 조각나 보여요.
   색 영역, 여백, 얇은 줄로 나눠요.
5. **기준을 숫자로 정한다.** 간격은 4의 배수, 글자 크기는 정해진 단계에서만,
   터치 영역은 44 이상.
6. **색은 직접 고른다.** 기본 팔레트를 그대로 쓰지 않아요. 순수 검정·네온 금지.
7. **움직임은 상태를 알릴 때만.** transform과 opacity만 건드려요.
8. **쉬움이 은폐가 되면 안 된다.** 줄이더라도 중요한 사실은 감추지 않아요.
   (수행평가 날짜, 알레르기 번호처럼 놓치면 곤란한 건 반드시 보이게)

## 우리 앱 규칙

### 간격
4의 배수만 써요. 2를 쓸 때는 선 두께나 미세 정렬처럼 이유가 있을 때만요.

    4  8  12  16  20  24  32  40

### 글자 크기
아래 단계에서만 골라요. 새 크기를 만들지 않아요.

| 쓰임 | 크기 |
|---|---|
| 화면 큰 제목 (홈 히어로) | 32 |
| 화면 제목 | 24 |
| 섹션 제목 | 18 |
| 본문·목록 | 15 |
| 보조 설명 | 13 |
| 라벨·캡션 | 12 |

### 글꼴
- 한글·일반 글씨: **Pretendard** (Regular 400 / Medium 500 / SemiBold 600 / Bold 700)
- 숫자·영문만 있는 자리: **Outfit** (`<Text numeric>`)
- 800 이상은 쓰지 않아요. 뭉툭해 보여요.

### 색
- 바탕은 연회색, 카드는 흰색. 다크모드 바탕은 `#0F0F12` (순수 검정 아님).
- 테두리 회색에는 테마 색을 살짝 섞어요 (틴티드 뉴트럴). `src/constants/themes.ts`
- 과목·카테고리는 고정된 파스텔 한 쌍(연한 바탕 + 진한 글씨). `src/constants/tones.ts`
- 과목 색은 **이름이 아니라 교과군**으로 정해요. NEIS가 "국어"가 아니라
  "공통국어2" "고전 읽기"처럼 선택과목 이름을 주거든요. `src/lib/subject.ts`
- 아이콘 타일만 선명한 색(`SOLID`)을 써요. 앱 아이콘은 테마 따라 안 바뀌니까요.
- 새 색을 넣으면 밝게·어둡게 모두 대비 4.5 이상인지 확인해요.

### 터치 영역
최소 44 x 44. 작아 보여야 하면 `hitSlop`으로 넓혀요.

### 팝업 (`src/components/sheet.tsx`)
"잠깐 하고 돌아올 일"은 화면을 갈아끼우지 말고 위에 띄워요. 로그인이 그래요.
뒤에 있던 게 안 사라지고, 닫으면 바로 원래 자리예요.
폰에서는 아래에서 올라오고(손잡이 표시), 넓은 화면에서는 가운데 창이 돼요.
좁은 화면에서 가운데 창을 띄우면 자판이 올라왔을 때 갈 곳이 없어요.

팝업을 효과(`useEffect`) 안에서 닫으라고 시키지 않아요. 조건으로 계산해요.
(`visible={열림 && !로그인됨}`) 효과로 닫으면 다음 화면 위에 그대로 덮여요.

### 달력 날짜 칠하기
날짜 숫자가 앉은 네모를 칠해요. 밑에 얇은 막대를 그리면 잘 안 보여요.
종류가 여럿이면 대각선 줄무늬로 나누고, **사이에 가는 흰 선**을 끼워요.
우리 파스텔들은 색조만 다르고 밝기가 거의 같아서(초록과 분홍의 밝기 차이가
0.000이에요) 맞붙여 놓으면 한 덩어리로 보이거든요.

색을 더 진하게 하는 건 안 돼요. 조금만 진해져도 그 위 숫자의 대비가
4.5 아래로 떨어져요. 우리 팔레트는 연한 바탕 + 진한 글씨 짝일 때만 읽혀요.
(선명한 색 + 흰 글씨도 재봤는데 12색 전부 4.5를 못 넘겨요)

색만으로 알리지 않아요. 일정이 있으면 숫자가 굵어지고, 두 개 이상이면
오른쪽 위에 개수를 적어요. 오늘도 마찬가지예요 — 오늘은 칸이 테마색으로
덮여서 일정 색이 안 보이거든요.

### 아이콘은 3D 그림으로

아이콘 자리에는 **입체로 그린 물체**를 놓아요. 선으로 그린 기호가 아니에요.

한동안 반대로 하고 있었어요. 담는 상자에 그라데이션과 광택, 색 그림자를
잔뜩 발라 입체인 척하고, 그 안에는 납작한 흰 기호를 넣었어요. 버튼만
번들거리고 정작 내용물은 평면이라 어딘가 어설퍼 보였어요.

지금은 이렇게 해요.

| | 예전 | 지금 |
|---|---|---|
| 상자 | 그라데이션 + 광택 + 색 그림자 | 연한 색 한 겹 (`tone().bg`) |
| 내용물 | 흰색 선 기호 | 3D 그림 |

그림은 [Microsoft Fluent Emoji](https://github.com/microsoft/fluentui-emoji)
(MIT)를 144px로 줄여서 `assets/emoji/` 에 담아뒀어요. 23개 216KB예요.
`src/components/emoji.tsx` 가 이름과 교과군을 이어줘요.

**입체감은 "누르는 것"에만 줘요.**
아이콘 상자는 보는 것이라 번들거리면 안의 그림을 방해해요. 버튼과 아바타는
누르는 것이라 살짝 솟아 보이면 눌러도 된다는 게 손에 먼저 읽혀요.
버튼은 세로 그라데이션 + 맨 윗줄 흰 선 한 겹 + 버튼 색 계열의 그림자예요.
윗면 절반에 흰 광택을 면으로 깔면 안 돼요. 가로로 긴 버튼에서는 광택이
끝나는 자리가 가로줄로 보여서 두 색이 겹친 것처럼 돼요. (동그란 것은 괜찮아요)

**선 기호를 그대로 쓰는 곳도 있어요.** 꺾쇠(`<` `>`), 체크, 휴지통, 탭바처럼
누르는 자리를 알려주는 UI 골격이에요. 여기에 3D를 넣으면 무겁고 지저분해요.
3D는 **내용과 분류**를 가리킬 때만 써요 — 과목, 급식, 달력, 쪽지, 빈 화면.

밝은 색 바탕에서 흐려 보이는 그림은 쓰지 않아요. 흰 말풍선과 흰 수첩을
넣었다가 바탕에 묻혀서 각각 파란 우편함과 노란 폴더로 바꿨어요.

### 아이콘 가운데 맞추기
담는 상자와 아이콘의 **크기 차이를 짝수로** 맞춰요.
상자 42에 아이콘 21이면 양옆 여백이 10.5가 되는데, 화면은 반 픽셀을
못 그려서 한쪽으로 밀려요. 눈에 확 띄진 않지만 묘하게 안 맞아 보여요.
`fitIcon(상자크기, 비율)` 이 알아서 짝수로 맞춰줘요. (`src/components/icon.tsx`)

### 움직임 (`src/components/motion.tsx`)
- `Tap` — 누르면 살짝 작아졌다 스프링으로 복귀. 폰에서는 약한 진동.
- `Reveal` — 화면에 뜰 때 아래에서 올라오며 등장. 순서대로 지연.
- `Pop` — 하나씩 톡 하고 커지며 등장 (0.86 → 1). 아이콘을 나란히 놓을 때.
- `Shimmer` — 아직 안 온 내용의 자리를 잡고 은은하게 밝아졌다 어두워져요.
- `Bump` — 숫자가 바뀔 때 살짝 커졌다 돌아와요. 안 읽은 개수처럼.
- `SlidingPill` — 선택 표시가 미끄러져 이동.
- 이것들 말고 새 애니메이션을 만들 때도 **transform과 opacity만** 건드려요.
- 통통 튀는 bounce는 쓰지 않아요. `Pop`의 0.14도 그래서 작게 잡은 거예요.

**목록은 순서대로 들어오게 해요.** 급식 메뉴, 시간표 교시, 달력 일정이
그래요. 한 줄에 45~50ms씩 미뤄요.

**내용이 갈릴 때는 key를 바꿔요.** 요일이나 반을 바꿨는데 글자만 조용히
갈리면 바뀐 걸 눈치채기 어려워요. `key={`${반}:${요일}:${교시}`}` 처럼
적어두면 줄이 새로 그려지면서 다시 올라와요.

**기다릴 때 빙글빙글 도는 동그라미를 쓰지 않아요.** `Loading` 이 들어올
내용 모양대로 자리를 잡아줘요. 화면이 덜 덜컹거리고 얼마나 올지도 보여요.

## 하지 말 것

- 테두리 카드 안에 테두리 카드
- 같은 모양 카드를 3개씩 나열
- 보라-파랑 네온 그라데이션
- 순수 검정(`#000`) 바탕
- 밝은 화면 색을 기계적으로 반전시킨 다크모드
- 화면마다 새로 만드는 글자 크기
- "확인", "제출" 같은 뭉뚱그린 버튼 글씨 (무엇을 하는지 적어요)
- 빈 화면에 "데이터 없음"만 두기
  (`Empty` 에 그림·다음에 할 일·버튼을 같이 넘겨요)
- 상자에 그라데이션과 광택을 발라 입체인 척하기 (그림을 입체로 만들어요)
- 없는 정보를 그럴듯하게 지어내기
  (NEIS 시간표에는 선생님 이름이 없어요. 지어내느니 안 보여줘요)

## 지금 상태 (2026-09-11 기준)

직접 코드를 세어본 결과예요. 점검표 네 가지를 모두 고쳤어요.

| 항목 | 상태 |
|---|---|
| 다크모드 순수 검정 안 씀 | 지킴 (`#0F0F12`) |
| 애니메이션이 transform·opacity만 | 지킴 |
| 틴티드 뉴트럴 | 지킴 |
| 과목 색 대비 4.5 이상 | 지킴 (11색 x 2모드 확인) |
| 간격 4의 배수 | 지킴 (153곳 전부. 선 두께·미세 정렬 1~2는 10곳) |
| 글자 크기 단계 | 지킴 (12 / 13 / 15 / 18 / 24 / 32 — 6종) |
| 터치 영역 44 이상 | 지킴 (칩·세그먼트·삭제·색상 버튼 모두 44) |
| 카드 걷어내기 | 지킴 (`<Card>` 사용처 0곳. 얇은 줄과 색 영역으로 나눠요) |

테두리가 남아 있는 곳은 카드가 아니라 **조작 부품**이에요.
칩·버튼·입력칸·태그·탭바·말풍선·달력 날짜 동그라미 — 눌러야 하는 것과
읽기만 하는 것을 구별해주는 테두리라서 남겨요.

## 참고할 곳

- 한국 앱: 토스, 배달의민족, 당근
- 레퍼런스 모음: [Dribbble](https://dribbble.com/), [21st.dev](https://21st.dev/)
- 한글 폰트: [눈누](https://noonnu.cc/)
